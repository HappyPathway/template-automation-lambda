[pytest]
markers =
    integration: marks tests as integration tests (deselect with '-m "not integration"')
testpaths =
    tests
python_files =
    test_*.py
    *_test.py
addopts =
    -v
    --strict-markers